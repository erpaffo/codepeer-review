$('.notification[data-id="<%= @notification.id %>"]').fadeOut('slow', function() { $(this).remove(); });

if ($('.notification:not(.read)').length === 0) {
  $('.notifications-link .notification-dot').remove();
}
