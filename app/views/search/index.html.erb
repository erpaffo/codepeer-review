<h1>Search</h1>

<%= form_with url: search_path, method: :post, local: true do |form| %>
  <%= form.label :query, 'Search for users or projects' %>
  <%= form.text_field :query %>
  <%= form.submit 'Search' %>
<% end %>
