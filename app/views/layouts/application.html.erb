<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "CodePeer Review" %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/dropzone/5.9.3/min/dropzone.min.css">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  <%= stylesheet_link_tag 'flash', media: 'all', 'data-turbolinks-track': 'reload' %>
  <script src="https://www.google.com/recaptcha/enterprise.js?render=6Ld66CwqAAAAANYIsBtGugIN0C9QtbpekQg9bjL2"></script>  
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/dropzone/5.9.3/min/dropzone.min.js"></script>
</head>

<body class="font-Inter bg-gray-900 text-gray-300">

  <div class="min-h-screen flex flex-col">
    <header class="bg-gray-800 shadow overflow-hidden">
      <div class="container mx-auto px-4 py-6 flex justify-between items-center">
        <h1 class="text-xl font-semibold text-gold">
          <%= link_to "CodePeer Review", user_signed_in? ? authenticated_root_path : unauthenticated_root_path %>
        </h1>
        <nav class="hidden md:flex space-x-4">
          <% if user_signed_in? %>
            <%= link_to "Profile", profile_path, class: "text-gray-200 hover:text-teal-300" %>
            <%= link_to "My Snippets", my_snippets_path, class: "text-gray-200 hover:text-teal-300" %>
            <%= link_to "My Projects", projects_path, class: "text-gray-200 hover:text-teal-300" %>
            <%= link_to "Notifications", notifications_path, class: "notifications-link text-gray-200 hover:text-teal-300 relative" %>
            <% if current_user.notifications.unread.any? %>
              <span class="notification-dot w-2 h-2 bg-red-600 rounded-full"></span>
            <% end %>
            <%= link_to 'Search', search_path, class: 'text-gray-200 hover:text-teal-300' %>
            <%= link_to "Logout", logout_path, method: :delete, class: "text-red-400 hover:text-red-300" %>
          <% else %>
            <%= link_to "Sign In", new_user_session_path, class: "text-gray-200 hover:text-teal-300" %>
            <%= link_to "Sign Up", new_user_registration_path, class: "text-gray-200 hover:text-teal-300" %>
          <% end %>
        </nav>
        <!-- Hamburger Menu for Mobile -->
        <div class="md:hidden">
          <button id="menu-toggle" class="text-gray-200 hover:text-teal-300 focus:outline-none">
            <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path>
            </svg>
          </button>
        </div>
      </div>
      <!-- Mobile Menu -->
      <div id="mobile-menu" class="hidden md:hidden bg-gray-800 py-4">
        <div class="container mx-auto px-4">
          <% if user_signed_in? %>
            <%= link_to "Profile", profile_path, class: "block text-gray-200 hover:text-teal-300 mb-2" %>
            <%= link_to "My Snippets", my_snippets_path, class: "block text-gray-200 hover:text-teal-300 mb-2" %>
            <%= link_to "My Projects", projects_path, class: "block text-gray-200 hover:text-teal-300 mb-2" %>
            <%= link_to "Notifications", notifications_path, class: "block text-gray-200 hover:text-teal-300 relative mb-2" %>
            <% if current_user.notifications.unread.any? %>
              <span class="notification-dot w-2 h-2 bg-red-600 rounded-full"></span>
            <% end %>
            <%= link_to 'Search', search_path, class: 'block text-gray-200 hover:text-teal-300 mb-2' %>
            <%= link_to "Logout", logout_path, method: :delete, class: "block text-red-400 hover:text-red-300 mb-2" %>
          <% else %>
            <%= link_to "Sign In", new_user_session_path, class: "block text-gray-200 hover:text-teal-300 mb-2" %>
            <%= link_to "Sign Up", new_user_registration_path, class: "block text-gray-200 hover:text-teal-300" %>
          <% end %>
        </div>
      </div>
    </header>

    <!-- Flash messages -->
    <div class="container mx-auto mt-4 px-4">
      <% flash.each do |key, message| %>
        <div class="flash-<%= key %>">
          <%= message %>
        </div>
      <% end %>
    </div>

    <%= yield %>

    <footer class="bg-gray-800 text-gray-400 text-center py-4 mt-8">
      <p>&copy; 2024 CodePeer Review. All rights reserved.</p>
    </footer>
  </div>

  <script>
    document.getElementById('menu-toggle').addEventListener('click', function() {
      var menu = document.getElementById('mobile-menu');
      menu.classList.toggle('hidden');
    });
  </script>
</body>
</html>
