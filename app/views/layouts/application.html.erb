<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "CodePeer Review" %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>
<body class="font-Inter bg-gray-900 text-gray-300">

  <div class="min-h-screen flex flex-col">
    <header class="bg-gray-800 shadow">
      <div class="container mx-auto px-4 py-6 flex justify-between items-center">
        <h1 class="text-xl font-semibold text-gold">
          <%= link_to "CodePeer Review", user_signed_in? ? authenticated_root_path : unauthenticated_root_path %>
        </h1>
        <nav class="flex space-x-4">
          <% if user_signed_in? %>
            <%= link_to "Dashboard", authenticated_root_path, class: "text-gray-200 hover:text-teal-300" %>
            <%= link_to "Profile", profile_path, class: "text-gray-200 hover:text-teal-300" %>
            <%= link_to "My Snippets", my_snippets_path, class: "text-gray-200 hover:text-teal-300" %>
            <%= link_to "My Projects", projects_path, class: "text-gray-200 hover:text-teal-300" %>
            <%= link_to "Logout", logout_path, method: :delete, class: "text-red-400 hover:text-red-300" %>
          <% else %>
            <%= link_to "Sign In", new_user_session_path, class: "text-gray-200 hover:text-teal-300" %>
            <%= link_to "Sign Up", new_user_registration_path, class: "text-gray-200 hover:text-teal-300" %>
          <% end %>
        </nav>
      </div>
    </header>

    <%= yield %>

    <footer class="bg-gray-800 text-gray-400 text-center py-4 mt-8">
      <p>&copy; 2024 CodePeer Review. All rights reserved.</p>
    </footer>
  </div>

  <script>
    const sidebarToggle = document.querySelectorAll('.sidebar-toggle');
    const sidebar = document.querySelector('.sidebar');
    const content = document.querySelector('.flex-1');

    sidebarToggle.forEach(toggle => {
      toggle.addEventListener('click', () => {
        sidebar.classList.toggle('-translate-x-full');
        content.classList.toggle('ml-16');
        content.classList.toggle('ml-64');
      });
    });
  </script>
</body>
</html>
