<h1>Verify OTP Code</h1>
<p>We have sent a code to your <%= current_user.two_factor_method == 'sms' ? 'phone' : 'email' %>. Please enter it below:</p>
<%= form_with url: verify_two_factor_auth_path, method: :post do %>
  <%= label_tag :otp_attempt, 'Code' %>
  <%= text_field_tag :otp_attempt %>
  <%= submit_tag 'Verify' %>
<% end %>
<p>If you didn't receive the code, you can <%= link_to 'send a backup email', send_backup_email_two_factor_auth_path, method: :post %>.</p>
